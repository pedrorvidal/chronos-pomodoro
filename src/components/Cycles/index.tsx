import styles from './styles.module.css';

export function Cycles() {
  return (
    <div className={styles.cycles}>
      <h1 className={styles.title}>Cycles Component</h1>
      <p className={styles.description}>This is the Cycles component.</p>
    </div>
  );
}
